# katharintai.github.io


### Prerequisites
You need Ruby, on macOS you probably run:
```
brew install ruby
``` 

Then install the dependencies inside the project folder run:
```
bundle install
```


### To run locally

In the project folder run:
```
bundle exec jekyll serve
```
